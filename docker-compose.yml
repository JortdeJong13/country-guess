services:
  mlflowserver:
    build: 
      context: ./
      dockerfile: ./mlflowserver/Dockerfile
    image: mlflowserver
    expose:
      - 5001
  # webapp:
  #   build:
  #     context: ./
  #     dockerfile: ./webapp/Dockerfile
  #   image: webapp
  #   ports:
  #     - '5000:5000'