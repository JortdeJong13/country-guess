services:
  mlflowserver:
    restart: always
    build: 
      context: ./
      dockerfile: ./mlflowserver/Dockerfile
    image: mlflowserver
    expose:
      - 5001
  webapp:
    build:
      context: ./
      dockerfile: ./webapp/Dockerfile
    image: webapp
    expose:
      - 5000